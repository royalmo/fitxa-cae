<% content_for :title, t(".title") %>

<section class="admin-page-heading">
  <h1 class="admin-page-title"><%= icon "user-plus", class: "admin-page-title-icon" %><span><%= t(".heading") %></span></h1>
</section>

<section class="panel">
  <%= render "form", employee: @employee %>
</section>
