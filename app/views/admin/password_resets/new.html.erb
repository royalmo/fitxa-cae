<% content_for :title, t(".title") %>

<section class="auth-card auth-card-narrow admin-auth-card">
  <header class="auth-header">
    <%= render "layouts/brand_mark", path: admin_login_path %>
    <h1><%= t(".heading") %></h1>
  </header>

  <section class="auth-panel">
    <div class="auth-panel-actions">
      <%= link_to admin_login_path, class: "auth-back-link" do %>
        <%= icon "arrow-left", class: "icon" %>
        <span><%= t("shared.back") %></span>
      <% end %>
    </div>

    <% flash.each do |type, message| %>
      <div class="flash flash-<%= type %>"><%= message %></div>
    <% end %>

    <%= form_with url: admin_password_reset_path, method: :post, class: "auth-form" do |form| %>
      <div class="field">
        <%= form.label :email, t(".email") %>
        <%= form.email_field :email, value: params[:email], autocomplete: "username", required: true %>
      </div>

      <%= form.submit t(".submit"), data: { submitting_label: t("shared.submitting.sending") } %>
    <% end %>
  </section>
</section>
