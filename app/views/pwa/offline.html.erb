<% content_for :title, t(".title") %>

<section class="page-intro">
  <div>
    <p class="eyebrow"><%= t("app.name") %></p>
    <h1><%= t(".heading") %></h1>
    <p><%= t(".body") %></p>
  </div>
</section>

<section class="clock-card offline-card">
  <p class="clock-label"><%= t("employee.dashboard.show.offline_ready") %></p>
  <%= link_to t(".retry"), root_path, class: "primary-clock" %>
</section>
