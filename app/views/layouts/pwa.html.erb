<!DOCTYPE html>
<html lang="<%= I18n.locale %>" data-pwa="fallback">
  <head>
    <%= render "layouts/head", manifest: true, theme_color: "#fff7f7" %>
  </head>

  <body class="fallback-shell" data-controller="submit-feedback">
    <main class="fallback-main">
      <%= yield %>
    </main>
  </body>
</html>
